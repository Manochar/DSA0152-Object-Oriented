#include <iostream>
using namespace std;
int main() {
    int n, sum = 0;
    cout << "Enter a positive integer: ";
    cin >> n;
    int i = 1;
    while (i <= n) {
        sum += i;
        ++i;
    }
    cout << "Sum of natural numbers up to " << n << ": " << sum;
    return 0;
}
